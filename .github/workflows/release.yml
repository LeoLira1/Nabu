name: Publish Android Release

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/release.yml

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Gerar projeto Android
        run: flutter create --platforms=android --project-name=nabu .

      - name: Configurar Android 7+ e acesso à internet
        run: |
          if [ -f android/app/build.gradle.kts ]; then
            sed -i 's/minSdk = flutter.minSdkVersion/minSdk = 24/' android/app/build.gradle.kts
          elif [ -f android/app/build.gradle ]; then
            sed -i 's/minSdkVersion flutter.minSdkVersion/minSdkVersion 24/' android/app/build.gradle
          fi
          manifest=android/app/src/main/AndroidManifest.xml
          if ! grep -q 'android.permission.INTERNET' "$manifest"; then
            sed -i '/<manifest /a\    <uses-permission android:name="android.permission.INTERNET"/>' "$manifest"
          fi
          grep -q 'android.permission.INTERNET' "$manifest"

      - name: Instalar dependências
        run: flutter pub get

      - name: Análise estática
        run: flutter analyze

      - name: Testes
        run: flutter test

      - name: Compilar APK de produção
        run: flutter build apk --release

      - name: Preparar APK
        run: cp build/app/outputs/flutter-apk/app-release.apk nabu-v0.4.0.apk

      - name: Publicar Nabu v0.4.0
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create v0.4.0 nabu-v0.4.0.apk \
            --repo "$GITHUB_REPOSITORY" \
            --target "$GITHUB_SHA" \
            --title "Nabu v0.4.0" \
            --notes "Adiciona setas retas e curvas com rotação, símbolos visuais, sincronização manual bidirecional e vários quadros independentes que podem ser criados, alternados e renomeados. Mantém ancoragem, imagens em retrato/paisagem e sincronização opcional com Turso."
