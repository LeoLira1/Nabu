name: nabu
description: Quadro visual infinito, local-first, com sincronização opcional no Turso.
publish_to: none
version: 0.3.0+3

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  libsql_dart: ^0.9.0+0.9.30
  image_picker: ^1.2.3
  flutter_secure_storage: ^11.1.1
  shared_preferences: ^2.5.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
